User / runtime data (not source code)
=====================================
data/      SQLite (memory.db), user-profile.json, etc. — paths.json → dirs.data, files.database
uploads/   Web UI file uploads — paths.json → dirs.uploads

Override folder names in paths.json if needed. Legacy repo-root data/ and uploads/ are copied here on startup when files are missing.

# JSON Schemas

Place JSON Schema files under `config/schemas/<category>/`. Paths in `config/skills.yaml` are relative to this directory.

Phase 2+ will validate tool arguments against these schemas.
